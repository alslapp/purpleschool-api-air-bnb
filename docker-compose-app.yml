version: '3.1'

services:

  top-api-air-bnb:
    build:
      context: .
      dockerfile: Dockerfile
    image: top-api-air-bnb:latest
    container_name: top-api-air-bnb
    restart: always
    ports:
      - 3338:3338
    env_file:
      - ./.env.prod
